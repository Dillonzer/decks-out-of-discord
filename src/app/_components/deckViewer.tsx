import * as React from 'react';
import { Decklist } from '../_types/apiResponses';

type DeckViewerProps = {
    deck : Decklist
}

export default function DeckViewer({deck} : DeckViewerProps) {
    //TODO: Build the decklist viewer
    return (
        <div>
        </div>
    )
}